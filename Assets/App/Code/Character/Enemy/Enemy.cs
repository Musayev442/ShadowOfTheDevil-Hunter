using UnityEngine;
using SotD.Characters;

namespace SotD.Characters.Enemy
{
    /// <summary>Concrete enemy base; specific enemies can subclass this.</summary>
    public class Enemy : Character
    {
        //[Header("Enemy")]
        /*[SerializeField] private EnemyStats enemyStats;

        protected override void Awake()
        {
            base.Awake();
            // Fetch/prepare any enemy-only modules here if needed later
        }

        public override void Initialize() { }
        public override void TakeDamage(DamageInfo dmg) { }
        public override void Die() { }
        */
    }
}
