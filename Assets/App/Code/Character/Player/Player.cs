using UnityEngine;

namespace SotD.Characters.Player
{
    /// <summary>Concrete player character. Extend with player-only data/behaviours.</summary>
    public class Player : Character
    {
        /*[Header("Player")]
        [SerializeField] private PlayerStats playerStats;
        [SerializeField] private PlayerInventory inventory;
        [SerializeField] private PlayerProgression progression;

        protected override void Awake()
        {
            base.Awake();
            // Fetch/prepare any player-only modules here if needed later
        }

        public override void Initialize() { }
        public override void TakeDamage(DamageInfo dmg) { }
        public override void Die() { }
        */
    }
}
